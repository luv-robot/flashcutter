# Flashcutter 试用用户使用说明

这份说明给试用用户使用，不解释系统架构和技术原理，只说明日常怎么操作。

## 1. Flashcutter 是做什么的

Flashcutter 用来把一条已经授权使用的原始广告视频，批量做成多个短视频广告变体。

你可以把它理解成一个广告视频生产工作台：

```text
上传原始视频
选择修改方法
批量生成变体
人工审核
下载可用成片
```

Flashcutter 不是通用剪辑软件，也不是投放平台。现阶段它主要帮助你更快做出一批可检查、可复盘、可交付的广告素材。

## 2. 使用前准备

请先准备：

```text
1. 已确认可以使用的原始视频。
2. 需要测试的广告方向或素材需求。
3. 可选：自己的配乐文件。
4. 可选：可重复使用的视频片段，例如开场、结尾、产品特写。
```

重要：素材授权请在上传前确认。Flashcutter 不负责判断某条素材是否可商用。

## 3. 登录

打开试用地址后，用分配给你的手机号和密码登录。

如果需要修改密码，可以在左侧用户区域展开账号操作，输入当前密码和新密码后提交。

## 4. 上传原始视频

进入「素材」页面。

你可以：

```text
上传本地视频
或导入视频 URL
```

上传后的原始视频会作为后续生产广告变体的种子视频。建议文件命名能让人看懂，例如：

```text
奶茶新品-窗边实拍-竖版.mp4
```

这样审核和下载时更容易识别。

## 5. 准备配乐

进入「配乐库」页面。

配乐分两类：

```text
系统公用库：系统预置，所有试用用户可见。
我的私有库：你自己上传，只给当前账号使用。
```

你可以先试听系统配乐。如果需要使用自己的音乐，上传到私有库即可。

当前试用版本的规则很简单：模板选择了配乐后，生成视频会用这首配乐替换原视频声音。

## 6. 准备视频片段

进入「视频片段」页面。

这里放可以重复使用的短视频片段，例如：

```text
开场 Hook
结尾 CTA
产品特写
氛围 B-roll
反应镜头
```

你可以上传自己的片段，并填写标题、类型、标签和说明。后续 AI 仿制生成的片段也会进入这里，和手动上传的片段一起管理。

## 7. 查看和调整模板

进入「模板」页面。

模板可以理解成“视频修改方法”，例如：

```text
快速开场 + 证据展示
静音字幕版
产品细节特写
开箱步骤
前后对比
```

内置模板不能直接覆盖。你可以基于内置模板保存一个自己的副本，再修改。

模板里常见设置包括：

```text
画面比例和尺寸
剪辑节奏
画面叠字
配乐
画面风格
左右镜像
转场和质感
审核重点
```

如果不确定某个模板是否合适，建议先用一条短视频跑少量变体，再看审核结果。

## 8. 创建一批广告变体

进入「变体生成」或生产相关页面。

新手优先使用页面顶部的「强开场批量生成」：

```text
1. 选择一条已授权的种子视频。
2. 填写产品名、卖点、禁用词和生成数量。
3. 点击“生成 AI 建议版”，系统会给出多条前三秒开场文字。
4. 你可以直接编辑，也可以用自己的每行一条文案替换 AI 建议。
5. 运行预检，通过后点击“一键入队”。
```

这个入口会自动使用强开场方案包，不需要先理解模板参数。适合先把一条素材扩成几十条开场不同的可审核变体。

模板向导适合更复杂的批量组合：

```text
1. 选择一个原始视频。
2. 选择一个或多个模板。
3. 检查本次生产设置。
4. 创建任务，或创建任务并加入渲染队列。
```

只创建任务：先把任务列出来，稍后再手动运行。

创建并加入渲染队列：立即排队开始生成视频。

建议试用时先选择 2-3 个模板，确认效果和速度后再扩大批量。

## 9. 查看队列和失败任务

进入「队列」或「任务」页面。

这里可以看到：

```text
正在排队的任务
正在渲染的任务
已完成任务
失败任务和失败原因
```

失败任务不会进入审核页。需要先在队列里查看原因，再决定是否重试或调整模板。

## 10. 审核生成结果

进入「审核」页面。

审核页按生产批次组织结果。一个批次通常对应：

```text
一次原始视频
一组模板
一批生成出来的广告变体
```

批次名称会尽量包含时间和原始素材名，方便你识别。

你可以逐条查看变体，并做决定：

```text
通过：这个视频可以交付或进入后续投放准备。
要求修改：这个方向可用，但还需要改。
拒绝：这个视频不适合继续使用。
丢弃：不进入后续处理。
```

如果选择「要求修改」，请尽量写清楚要改什么，例如：

```text
开场文字太弱，需要更直接。
产品出现太晚。
配乐不适合，换成更轻快的。
画面裁切挡住了主体。
```

这些反馈后续会作为再生产的输入。

## 11. 标记整个批次需要再生产

有时单条视频的问题不大，但整批都需要重新来一轮，例如：

```text
所有视频节奏都太慢。
配乐整体不适合。
叠字方向需要统一重写。
模板选错了。
```

这时可以把整个批次标记为需要再生产。

系统会保留原批次和后续修改轮次的关系，方便之后追溯“为什么改、改了几轮”。

## 12. 打包下载过审视频

当一个批次里已经有通过审核的视频，可以在审核页使用「打包下载」。

打包内容包括：

```text
原始种子视频
这个批次里所有已通过的视频
```

下载前系统会提示预计文件大小。文件较大时请确认网络和磁盘空间足够。

这个 zip 包可以作为连接广告平台之前的过渡交付物，直接发给合作方或投放同事检查。

## 13. AI 仿制生成

AI 相关功能是独立实验能力，不是当前主生产流程的必经步骤。

它的目标是：用一张参考图或一段参考视频，生成一个相似风格的新短片段，然后进入「视频片段」库。

现阶段请把它看作补充素材的方法，而不是自动生成完整广告的方法。

## 14. 建议的试用顺序

第一次试用建议按这个顺序：

```text
1. 登录。
2. 上传 1 条已授权原始视频。
3. 试听系统配乐。
4. 选择 2-3 个模板生成变体。
5. 在队列页确认任务完成。
6. 在审核页逐条审核。
7. 通过 1-2 条视频。
8. 打包下载。
9. 记录需要修改的问题。
```

第二次试用再尝试：

```text
上传自己的配乐
上传自己的视频片段
修改模板副本
标记批次需要再生产
```

## 15. 反馈时请尽量说明

如果遇到问题，请尽量记录：

```text
发生在哪个页面
原始视频名称
批次名称或任务编号
你点击了什么
你期待看到什么
实际发生了什么
是否有错误提示
```

越具体，越容易定位和修复。
